---
description: Dispatch a Fleet swarm — fan-out parallel work via claude-flow.
argument-hint: <task> [--agents N] [--topology parallel|hive-mind] [--strategy development|analysis|devops|documentation]
---

Call `mcp__fleet__dispatch_swarm` with the parsed arguments. Default agents=20, topology=parallel, strategy=development.
