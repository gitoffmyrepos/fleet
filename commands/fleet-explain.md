---
description: Return the citation chain for a Fleet task.
argument-hint: <task_id>
---

Call `mcp__fleet__explain` with `{task_id: "$ARGUMENTS"}`. Render the chain as an ordered list with each step's kind + summary.
